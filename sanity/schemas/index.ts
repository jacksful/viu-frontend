import siteSettings from "./siteSettings";
import page from "./page";
import {
  heroSection,
  statsBar,
  featureSection,
  faqSection,
  ctaBanner,
} from "./sections";

export const schemaTypes = [
  siteSettings,
  page,
  heroSection,
  statsBar,
  featureSection,
  faqSection,
  ctaBanner,
];
